### Quiz Start ###

- **QUESTION 1**: In the context of analyzing F1 race data, which Python method would you use to open a file containing lap times for reading?
  - A) `open(filename, 'w')`
  - B) `open(filename, 'r')`
  - C) `open(filename, 'a')`
  - D) `open(filename, 'b')`
  - **Correct Answer: B**

- **QUESTION 2**: When fetching stock market data for analysis, which Python method is used to read the content of a file line by line?
  - A) `file.read()`
  - B) `file.readlines()`
  - C) `file.readline()`
  - D) `file.readchunk()`
  - **Correct Answer: C**

- **QUESTION 3**: For developing an investment portfolio app, which of the following correctly demonstrates how to open and read a file containing stock prices in Python?
  - A) 
    ```python
    with open('stock_prices.txt', 'r') as file:
        data = file.read()
    ```
  - B) 
    ```python
    with open('stock_prices.txt', 'w') as file:
        data = file.read()
    ```
  - C) 
    ```python
    with open('stock_prices.txt', 'a') as file:
        data = file.read()
    ```
  - D) 
    ```python
    with open('stock_prices.txt', 'b') as file:
        data = file.read()
    ```
  - **Correct Answer: A**

- **QUESTION 4**: While creating interactive dashboards for visualizing stock market trends, which method would you use to read all lines of a file into a list in Python?
  - A) `file.read()`
  - B) `file.readlines()`
  - C) `file.readline()`
  - D) `file.readall()`
  - **Correct Answer: B**

- **QUESTION 5**: In the context of healthcare data analysis, which Python code snippet correctly opens a file named `health_records.csv` in read mode and prints its content?
  - A) 
    ```python
    with open('health_records.csv', 'r') as file:
        print(file.read())
    ```
  - B) 
    ```python
    with open('health_records.csv', 'w') as file:
        print(file.read())
    ```
  - C) 
    ```python
    with open('health_records.csv', 'a') as file:
        print(file.read())
    ```
  - D) 
    ```python
    with open('health_records.csv', 'b') as file:
        print(file.read())
    ```
  - **Correct Answer: A**

### Quiz End ###
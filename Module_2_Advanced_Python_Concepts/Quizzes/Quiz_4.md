### Quiz Start ###

- **QUESTION 1**: When analyzing F1 race data, you need to store lap times for each driver. Which data structure would be more efficient for storing lap times that won't change?
  - Option A: List, because it allows modification of lap times.
  - Option B: Tuple, because it is immutable and ensures data integrity.
  - Option C: List, because it is faster to access.
  - Option D: Tuple, because it can store more data.

- **QUESTION 2**: You're building an investment portfolio app that tracks the performance of different stocks. For storing historical prices that are rarely modified, which data structure is more appropriate?
  - Option A: List, because it allows easy updates.
  - Option B: Tuple, because it is immutable and can ensure the historical data is not accidentally altered.
  - Option C: List, because it has more methods for data manipulation.
  - Option D: Tuple, because it uses less memory.

- **QUESTION 3**: In a web development project for finance, you need to store a set of fixed configuration settings. Which data structure would you choose?
  - Option A: List, because it allows dynamic changes.
  - Option B: Tuple, because it is immutable and secure.
  - Option C: List, because it is faster to iterate.
  - Option D: Tuple, because it supports more data types.

- **QUESTION 4**: For visualizing stock market trends in an interactive dashboard, you need to store a fixed sequence of dates and corresponding stock prices. Which data structure should you use?
  - Option A: List, because it allows modification.
  - Option B: Tuple, because it can store immutable data pairs efficiently.
  - Option C: List, because it is easier to append new data.
  - Option D: Tuple, because it is faster to iterate over.

- **QUESTION 5**: When automating stock market analysis, you need to ensure that the historical data remains unchanged throughout the analysis process. Which data structure is preferable?
  - Option A: List, because it allows easy updates.
  - Option B: Tuple, because it ensures data immutability.
  - Option C: List, because it has built-in functions for data manipulation.
  - Option D: Tuple, because it consumes more memory.

### Quiz End ###
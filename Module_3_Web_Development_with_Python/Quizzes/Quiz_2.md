### Quiz Start ###

- **QUESTION 1**: In a Flask application for visualizing F1 race data, which of the following is the correct way to define a route for the home page?
  - A) `@app.route('/home')`
  - B) `@app.route('/')`
  - C) `@app.route('/index')`
  - D) `@app.route('/main')`

- **QUESTION 2**: When developing an investment portfolio app using Django, how would you define a route in `urls.py` to display the portfolio?
  - A) `path('portfolio/', portfolio_view)`
  - B) `path('portfolio/', views.portfolio)`
  - C) `path('portfolio/', show_portfolio)`
  - D) `path('/portfolio', views.portfolio)`

- **QUESTION 3**: For a Flask application aimed at automating stock market analysis, what would be the appropriate way to define a route that triggers the analysis process?
  - A) `@app.route('/analyse', methods=['GET', 'POST'])`
  - B) `@app.route('/start_analysis', methods=['POST'])`
  - C) `@app.route('/automate', methods=['GET'])`
  - D) `@app.route('/analyze', methods=['POST'])`

- **QUESTION 4**: In a Django project for creating interactive dashboards, which of the following correctly defines a URL pattern for a dashboard view in `urls.py`?
  - A) `path('dashboard/', views.dashboard_view)`
  - B) `path('dashboard/', dashboard)`
  - C) `path('dashboard/', views.dashboard)`
  - D) `path('/dashboard', views.show_dashboard)`

- **QUESTION 5**: How would you define a route in a Flask application for building an F1 race track simulator, specifically for a page that lets users input track details?
  - A) `@app.route('/input_track_details', methods=['GET', 'POST'])`
  - B) `@app.route('/track_details', methods=['POST'])`
  - C) `@app.route('/input_track_data', methods=['GET'])`
  - D) `@app.route('/track_input', methods=['POST'])`

### Quiz End ###